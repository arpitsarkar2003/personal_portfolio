/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["img.freepik.com","i.ibb.co","via.placeholder.com","encrypted-tbn0.gstatic.com"],
    }
};

export default nextConfig;
